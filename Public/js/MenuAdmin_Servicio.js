'use strict';

let getUsuario = () => {
    let usuario;
    usuario = JSON.parse(localStorage.getItem('cliente'));
    return usuario;
}

let getNombreUsuario = () => {
    let nombre;
//Conectar al api
    return nombre;
}

let getApeliidoUsuario = () =>{
    let apellido;
//Conectar al api
    return apellido;
}